export default function Backup() {
    return (
        <></>
        // <html lang="en">
        //     <body className={`${geistSans.variable} ${geistMono.variable} antialiased h-screen overflow-hidden`}>
        //         <AuthHydrator isAuthenticated={hasRefreshToken} />

        //         {/* Full page flex column */}
        //         <div className="flex flex-col h-full">
        //             {/* Header is part of flow now, not fixed */}
        //             <Header className="w-full flex-shrink-0" />

        //             {/* Content area */}
        //             <div className="flex flex-1 overflow-hidden">
        //                 {/* Left Sidebar (always scrollable) */}
        //                 <aside className="w-64 h-full overflow-y-auto border-r border-gray-200">
        //                     <LeftSidebar />
        //                 </aside>

        //                 {/* Main content + everything else scroll together */}
        //                 <main className="flex-1 h-full overflow-y-auto">{children}</main>
        //             </div>
        //         </div>
        //     </body>
        // </html>
    );
}
